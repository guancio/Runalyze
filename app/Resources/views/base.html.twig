<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}{% endblock %}</title>
        {% block stylesheets %}
            {% set css_assets = app.debug ? app_assets.css.dev : app_assets.css.prod %}

            {% for asset in css_assets %}
                <link rel="stylesheet" href="{{ asset(asset) }}" />
            {% endfor %}
        {% endblock %}
                {% block javascripts %}
            {% set js_assets = app.debug ? app_assets.js.dev : app_assets.js.prod %}

            {% for asset in js_assets %}
                <script src="{{ asset(asset) }}"></script>
            {% endfor %}
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body id="installer">
        <div id="headline">
	<a class="tab logo" href="http://www.runalyze.de/" target="_blank">Runalyze {{ version }}</a>
	<a class="tab right" href="login.php"><i class="fa fa-fw fa-sign-in"></i> {% trans %}Please login{% endtrans %}</a>
        </div>
        <script>(function($){$(document).ready(function(){ Runalyze.init(); });})(jQuery);</script>
       
        <div id="ajax-outer">
            <div id="ajax" class="panel" style="display:block;">
            <div class="panel-heading">
                    <div class="panel-menu">
                            <ul>
                                    <li class="triggered"><a href="">English</a></li>
                            </ul>
                    </div>
                    <h1>Runalyze {{ version }} - {% trans %}Please login{% endtrans %}</h1>
            </div>
            <div class="panel-content">
            <div class="installer-window-container">
                {% block body %}{% endblock %}
            </div>

            <div id="login-panel">
                <a id="login-link" href="/"><i class="fa fa-fw fa-lg fa-sign-in"></i> {% trans %}Login{% endtrans %}</a>
                <a id="register-link" href="{{ path('register_route') }}" onclick="show('reg')"><i class="fa fa-fw fa-lg fa-user"></i> {% trans %}Create a new account{% endtrans %}</a>
                <a id="password-link" href=""><i class="fa fa-fw fa-lg fa-lock"></i> {% trans %}Forgot your password?{% endtrans %}</a>
            </div>

            <p class="text"></p>
            <p class="text small c login-window-stats">
                    Until now <strong>4210</strong> athletes are registered and have logged <strong>4.522.383,96&nbsp;km</strong>.	<br>
                    <strong>8</strong> athletes are online.<br>
            </p>

            {{ include('RunalyzeCoreBundle:Default:footer-text.html.twig') }}

            </div>
            </div>
    </div>
    </body>
</html>
